# P3 Single-server AmneziaWG Implementation Plan

Status: In progress. P3.1 through P3.3 software boundaries were accepted on
2026-08-24. The P3.4 package-software boundary and P3.5 read-only preflight are
implemented. Exact-router package/procd/boot/LKG behavior, live VPS and
handshake gates remain open.

## Goal

Qualify one GL-MT6000 and one VPS as a `single-site core-ready` system. P3
combines the P2 production dataplane with the pinned AWG2/OpenWrt tuple, while
keeping WAN as the default route and VPN-class traffic fail-closed.

## Boundaries

- No production API or web UI; P4 owns the local control plane.
- No mobile peer lifecycle; P8 owns the versioned forced-command protocol and
  one-time profile delivery.
- No multi-server failover; P9 owns that field gate.
- Secret bytes never enter Git, command-line arguments, logs, inventory or
  support artifacts.
- Router and VPS mutation requires explicit target inventory, strict SSH host
  key verification, a recovery path and a reviewed commit-confirm boundary.

## Implementation sequence

### P3.1 Restricted server-agent foundation

- Preserve `server-agent version --json`.
- Add `server-agent health --json` as a local, process-scoped liveness command.
- Expose no HTTP listener, shell execution, configuration mutation or secret
  input in this slice.
- Reject every unimplemented operation and malformed argument list.

Acceptance: stable schema-versioned JSON, explicit `scope=process`, exact CLI
allowlist, encode failure is non-zero, Go unit tests and the repository verify
batch pass.

### P3.2 Pinned VPS deployment

- Define a non-secret inventory and immutable deployment bundle contract.
- Implement idempotent install/upgrade, restricted SSH access, service unit,
  IP forwarding/firewall configuration and compensation on partial failure.
- Add AWG interface, handshake and egress health without exposing key material.

Acceptance: two applies converge, a failed step compensates to the previous
state, the admin surface has no public HTTP port, and password/root SSH are
disabled after the recovery account is proven.

Implemented software boundary:

- strict, non-secret JSON bundle with explicit adapter pin, complete public AWG2
  parameters, local artifact hashes, a post-parse semantic seal and a
  non-deployable sanitized-example bit;
- pre-mutation snapshot, explicit upgrade intent, compensation token and a
  second recovery-access check before SSH hardening;
- separate AWG service/interface/forwarding/NAT/listener/handshake/DNS/TLS/
  egress health with two-success/three-failure hysteresis.

The public-listener and SSH-hardening acceptance checks still require the real
VPS and its out-of-band recovery console. Unit tests do not claim those field
results.

### P3.3 Router peer and endpoint routing

- Install the exact verified AWG2 packages on GL-MT6000 using the existing
  rollback-safe hardware smoke as the precondition.
- Configure `route_allowed_ips=0` and an endpoint host route through WAN before
  tunnel activation.
- Bind the real tunnel to the P2 per-server mark/table slot without changing
  the system default route.

Acceptance: tunnel absence retains terminal fail-closed routes; endpoint and
router management remain reachable through WAN.

Implemented software boundary:

- strict peer spec and argv-only operation plan for one netifd `interface`
  using `proto=amneziawg` and one `amneziawg_<interface>` peer;
- mandatory cleanup of stale UCI private-key, PSK and `fwmark` options,
  `route_allowed_ips=0`, `nohostroute=1`, an explicit WAN host route and no
  change to the `main` default route;
- P2 fail-closed policy publication before peer mutation, snapshot restore on
  failure, full idempotent UCI reconciliation even for the same spec, and
  `Available=true` only after two structured health successes;
- a root-owned `private_key_file` source and ephemeral tmpfs setconf file with
  signal/exit cleanup in the project-owned netifd helper.

Exact APK installation, kernel/UAPI loading, endpoint reachability and the
router-to-VPS handshake remain hardware gates.

### P3.4 Router package lifecycle

- Package the `linux/arm64` `routerd` binary as
  `routerd_<version>_aarch64_cortex-a53.apk`.
- Verify install, upgrade, remove, UCI preservation, boot ordering and LKG
  recovery on the exact firmware/kernel tuple.

Implemented software boundary:

- pinned Go 1.26.6 cross-build of a static `linux/arm64` binary with stable
  source provenance and exact `aarch64_cortex-a53` APK metadata/payload;
- procd init contract with fixed late start, bounded respawn and no network
  mutation in the init script;
- isolated SDK-host r1-to-r2 install/upgrade/remove checks that preserve
  package-unowned UCI and dataplane fixtures, plus two clean byte-identical SDK
  output trees.

The isolated lifecycle uses `apk --no-scripts` because the payload is ARM64.
Maintainer scripts are inspected in APK metadata, but the ARM binary, procd,
boot ordering and LKG recovery are not executed. Those remain physical P3.5
gates on the exact router.

### P3.5 Physical acceptance

- Run behind the existing ISP router/ONT before any primary-WAN cutover.
- Repeat selective egress, DNS/IPv6/QUIC, MTU, AWG-down packet capture, reboot,
  Ethernet recovery and package lifecycle checks.
- Record CPU/RAM/flash and throughput as measurements, not assumptions.

Implemented preparation:

- a sanitized, deliberately non-executable acceptance inventory;
- strict-host-key, read-only router preflight for the exact board, OpenWrt,
  kernel/ABI, WAN endpoint/management route invariants and optional fresh AWG2
  handshake;
- redacted JSON evidence and a guarded temporary AWG2 package smoke runbook.

No physical router or VPS command was run. The preflight remains blocked until
the external inputs below are supplied and independently verified.

Exit gate: one physical router and one real VPS pass the complete P3 evidence
bundle; only then may the project claim `single-site core-ready`.

## External inputs still required

- Resettable GL-MT6000, exact firmware/kernel snapshot, wired recovery path and
  strict `known_hosts` material.
- One VPS inventory, recovery account and out-of-band provider console.
- Approved public endpoint and non-conflicting tunnel IPv4/IPv6 ranges.
- Secret key material supplied outside Git after the deployment contract is
  implemented and reviewed.

## Validation batch for P3.2 and P3.3

1. `go test ./internal/servers/... ./internal/system/openwrt/... ./internal/routing/iprule ./internal/dataplane`
2. `tests/openwrt-sdk/assert-awg2-helper.sh` and the focused OpenWrt package Pester suite
3. Two clean pinned OpenWrt SDK builds with byte comparison
4. `scripts/dev.ps1 -Command verify`
5. Independent security and QA review of bundle trust, secret handling,
   compensation, netifd schema and P2 fail-closed ordering

These checks do not change a live router, VPS, Windows route or VPN state.

## P3.4 and P3.5 preparation evidence

- The pinned SDK produces the exact r2 `routerd` APK and an r1 lifecycle
  fixture, validates both APKs and compares two clean output trees
  byte-for-byte.
- The isolated package lifecycle passes install, upgrade and remove checks
  while retaining package-unowned mock UCI/dataplane state.
- Pester fake-transport coverage verifies the P3.5 inventory, strict SSH,
  route, handshake, evidence-redaction and AWG2 smoke rollback contracts. The
  security fix-audit passed 24 tests without real SSH.
- Hosted commit `9c5903c3ff1d474a3112a20d784d9ab128afad1a` passed
  [CI run 32728418853](https://github.com/n1ck8131/home_gateway/actions/runs/32728418853),
  [OpenWrt QEMU run 32728418833](https://github.com/n1ck8131/home_gateway/actions/runs/32728418833)
  and [OpenWrt SDK run 32728418835](https://github.com/n1ck8131/home_gateway/actions/runs/32728418835).
- The SDK run uploaded `verified-openwrt-outputs` artifact ID `9521547381`;
  exact package hashes remain recorded in `docs/COMPATIBILITY.md` beyond the
  seven-day artifact retention window.
- No live VPS, exact-router, ARM/procd/boot/LKG or hardware acceptance result is
  claimed.

## P3.1 evidence

- Commit: `bdaec0b30f3152c47f37cca7f88f663b8b310976`
- Local: focused Go tests and the full repository `verify` batch passed
- Independent security review: clean after exact-allowlist and intended-commit
  checks
- Hosted: [CI run 32700597504](https://github.com/n1ck8131/home_gateway/actions/runs/32700597504),
  [OpenWrt QEMU run 32700597472](https://github.com/n1ck8131/home_gateway/actions/runs/32700597472)
  and [OpenWrt SDK run 32700597475](https://github.com/n1ck8131/home_gateway/actions/runs/32700597475)
  passed

## P3.2 and P3.3 software evidence

- Full local `scripts/dev.ps1 -Command verify` passed after the scoped `os.Root`
  bundle-loader fix: all Go tests, 41 Pester tests, static analysis, vulnerability,
  secret, governance and application-build checks passed.
- Focused OpenWrt package Pester passed 10 tests; helper assertions, shell syntax
  and pinned ShellCheck passed.
- Two clean pinned OpenWrt SDK trees were byte-identical. The `r2` tools APK is
  33,118 bytes with SHA-256
  `2eae2da601281c8ece9bf86bb6245d1af42fa736f0b738e030dc2014a0930ff9`.
- The final Ubuntu reproduction of `make PWSH=./.tools/pwsh/pwsh verify` passed,
  including 41 Pester tests, Go tests, static analysis, vulnerability, secret,
  ShellCheck, governance and application-build checks.
- Hosted evidence for commit `52a896717e0f01359d6eaedb4db9d9e949ac5000`:
  [CI run 32712734828](https://github.com/n1ck8131/home_gateway/actions/runs/32712734828),
  [OpenWrt QEMU run 32712734929](https://github.com/n1ck8131/home_gateway/actions/runs/32712734929)
  and [OpenWrt SDK run 32712734846](https://github.com/n1ck8131/home_gateway/actions/runs/32712734846)
  passed.
- Independent QA and security fix-audits are clean. No live VPS, exact-router,
  AWG handshake or hardware acceptance is claimed.
